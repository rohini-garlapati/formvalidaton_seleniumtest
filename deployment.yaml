apiVersion: apps/v1
kind: Deployment
metadata:
  name: formvalidation-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: formvalidation
  template:
    metadata:
      labels:
        app: formvalidation
    spec:
      containers:
        - name: formvalidation
          image: rohinigarlapati/formvalidationimage:v8
          ports:
            - containerPort: 5000
